pipeline {	
	agent any	
    
    tools {	    
		maven 'my-maven'		
		jdk 'my-jdk'	
	}	
	stages {        
		stage('Git'){			
			steps {git url:'https://github.com/Visweshvar-UST/jenkins-microservices', branch:'department-s1'}			
            }		
		stage('Build'){			
			steps {bat "mvn clean install -DskipTests"}		
		}		
		stage('Test'){			
			steps{bat "mvn test"}		
            }		
		stage('Deploy') {			
			steps { bat "docker build -t department-s1-image ."			    
			        bat "docker run -p 8081:8081 -d --name department-s1-container department-s1-image"}		
		}	
    }
}