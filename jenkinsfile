pipeline {	
	agent any	
    
    tools {	    
		maven 'my-maven'		
		jdk 'my-jdk'	
	}	
	stages {        
		stage('Git'){			
			steps {git url:'https://github.com/Visweshvar-UST/jenkins-microservices', branch:'service-registry'}			
            }		
		stage('Build'){			
			steps {bat "mvn clean install -DskipTests"}		
		}		
		stage('Test'){			
			steps{bat "mvn test"}		
            }		
		stage('Deploy') {			
			steps { bat "docker build -t service-registry-image ."			    
			        bat "docker run -p 8761:8761 -d --network app-network --name registry-sr service-registry-image"}		
		}	
    }
}